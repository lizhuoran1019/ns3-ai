# 采用显式共享内存会话生命周期

ns3-ai 将为每个共享内存会话代际建模为显式会话生命周期，而非扩展现有单侧完成标志或仅依赖有界等待。该生命周期区分握手机制、运行通信、关闭握手、关闭完成和终端错误结论，使 C++ 和 Python 两端能够一致地诊断正常完成、过期代际、协议不匹配、超时、对端死亡和中止执行。

本决策将会话状态与对端状态分离：对端状态仍然关于本地邮箱同步，而会话状态记录一个代际的共享生命周期结论。
