# 采用 Python 消息接口包装层

ns3-ai 将通过一个 Python 消息接口包装层（而非每个生成的 pybind 消息接口）作为共享内存消息接口的规范 Python 生命周期 API。

生成的 pybind 消息接口继续负责暴露载荷对象以及 Python 所需的底层邮箱和生命周期操作。Python 消息接口包装层提供稳定的面向 Python 的生命周期词汇，包括就绪等待、会话身份、关闭握手参与和结构化生命周期错误。

这使得共享内存会话生命周期语义集中在一个 Python API 边界内，同时允许生成的绑定保持载荷特定。
